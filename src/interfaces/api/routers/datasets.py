"""
Router pour les endpoints datasets (STUB)  
TODO: Implémenter les endpoints dataset
"""

from fastapi import APIRouter

router = APIRouter(prefix="/datasets", tags=["datasets"])

# TODO: Implémenter les endpoints
# - POST /datasets (add dataset)
# - GET /datasets/publisher/{name} (get by publisher)
# - GET /datasets/tests (get test datasets) 